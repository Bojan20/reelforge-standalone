[package]
name = "rf-ingest"
version = "0.1.0"
edition = "2021"
description = "FluxForge Universal Ingest System - Adapters for any slot engine"
license = "MIT"

[dependencies]
rf-stage = { path = "../rf-stage" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
toml = "0.8"
regex = "1.10"
