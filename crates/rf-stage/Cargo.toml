[package]
name = "rf-stage"
version = "0.1.0"
edition = "2021"
description = "FluxForge Stage System - Universal slot game phase definitions"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "stage_bench"
harness = false
