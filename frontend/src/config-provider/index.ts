/**
 * ReelForge ConfigProvider Module
 *
 * Global configuration context.
 *
 * @module config-provider
 */

export {
  ConfigProvider,
  ConfigContext,
  useConfig,
  useTheme,
  useLocale,
  useSize,
  type ConfigProviderProps,
  type ConfigContextValue,
  type ThemeMode,
  type ThemeConfig,
  type LocaleConfig,
  type ComponentSize,
} from './ConfigProvider';
